class Notifier {
    static send = null;
    static set(channel, ownerid) {
        Notifier.send = (message) => {
            channel.send(`<@${ownerid}>, ${message}`);
        }
    }
}

module.exports = Notifier;